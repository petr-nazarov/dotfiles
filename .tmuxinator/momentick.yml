name: momentick
root: ~/Projects/Momentick
startup_window: app
windows:
  - Projects: 
  - sdk:
      layout: main-horizontal
      root:  ~/Projects/Momentick/momentick-sdk
      panes:
        - main:
        - run: 
          - export DOPPLER_TOKEN=$DOPPLER_TOKEN_MOMENTICK
  - iac:
      layout: main-horizontal
      root:  ~/Projects/Momentick/momentick-iac
      panes:
        - main:
        - run: 
          - export DOPPLER_TOKEN=$DOPPLER_TOKEN_MOMENTICK
  - api:
      layout: main-horizontal
      root:  ~/Projects/Momentick/momentick-api
      panes:
        - main:
        - run: 
          - export DOPPLER_TOKEN=$DOPPLER_TOKEN_MOMENTICK
  - app:
      layout: main-horizontal
      root:  ~/Projects/Momentick/momentick-app
      panes:
        - main:
        - run: 
          - export DOPPLER_TOKEN=$DOPPLER_TOKEN_MOMENTICK
  - setup_machines:
      layout: main-horizontal
      root:  ~/Projects/Momentick/setup_machines
      panes:
        - main:
        - run: 
          - export DOPPLER_TOKEN=$DOPPLER_TOKEN_MOMENTICK
  - github-actions:
      layout: main-horizontal
      root:  ~/Projects/Momentick/momentick-github-actions
      panes:
        - main:
        - run: 
          - export DOPPLER_TOKEN=$DOPPLER_TOKEN_MOMENTICK
